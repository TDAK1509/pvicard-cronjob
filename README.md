# To run for lazada

```
python lazada.py \
	 --lazada-app-key="$LAZADA_APP_KEY" \
	 --lazada-app-secret="$LAZADA_APP_SECRET" \
	 --lazada-refresh-token="$LAZADA_REFRESH_TOKEN" \
	 --pvicard-api-token="<pvicard.com api token>"
```
